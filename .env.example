# Site Configuration
NEXT_PUBLIC_SITE_URL=https://dreamstudio.dev

# Analytics (Vercel Analytics - auto-configured on Vercel)
# No environment variables needed for Vercel Analytics
# It's automatically enabled when deployed to Vercel

# Contact Form API
NEXT_PUBLIC_CONTACT_API_URL=
CONTACT_API_SECRET=

# Email Configuration (if using direct email)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Twenty CRM Integration (optional)
# Get your API key from Twenty: Settings → APIs & Webhooks → Create key
# For cloud version use: https://api.twenty.com
# For self-hosted use your own domain URL
TWENTY_API_KEY=
TWENTY_API_URL=

# Error Tracking (Sentry)
# Get your DSN from: https://sentry.io/settings/projects/your-project/keys/
# Only needed in production - errors are logged to console in development
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# Environment
NODE_ENV=production
